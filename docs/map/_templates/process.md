---
type: process
universe: live
status: stub
verified:
branch:
source:
consumes: []
produces: []
---

# <process name>

<One sentence: the movement, not the nouns it moves. If this process is not
one that actually runs or is actually followed, it does not belong on this
shelf — the form admits only real movements.>

## Input → Movement → Output

**Input** — <what must exist before this starts, as paths or object cards>

**Movement** — <what happens, in one sentence>

**Output** — <what exists afterwards that did not before>

## Why this shape

<What breaks if the obvious shortcut existed. If nothing does, this process
probably is the shortcut and should be deleted rather than documented.>

## Steps

1. <step> — `path:line`
2. <step> — `path:line`

<Numbered, each citing the file that performs it. A step with no citation is
a belief about the system, not a description of it.>

## If you change this

**Hits**
- <first-order consequence> — `path:line`

**Does not hit**
- <the obvious next noun that is the WRONG one, and why it is untouched>

## Surfaces

| Who | Reads | Writes |
|---|---|---|
| <who runs it> |  |  |
| tests | `tests/test_<x>` |  |

## See

- **consumes** — <links to the object cards this reads>
- **produces** — <links to the object cards this writes>
- `<the source file this card cites>`
